# Cloudflare Pages Configuration
# Note: For static sites, Cloudflare Pages handles deployment automatically
# This file is optional - Pages will use dashboard settings

name = "portfolio"
compatibility_date = "2024-01-01"

# For Cloudflare Pages, you typically don't need this file
# Pages uses the build settings from the dashboard:
# - Build command: npm run build
# - Build output directory: dist
# - Framework preset: Vite
